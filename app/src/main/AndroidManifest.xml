<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bmc" >

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".setting.SettingCreateActivity"
            android:label="@string/title_activity_setting" >
        </activity>
        <activity
            android:name=".job.JobListActivity"
            android:label="@string/title_activity_job_list" >
        </activity>
        <activity
            android:name=".bucket.BucketListActivity"
            android:label="@string/title_activity_bucket_list" >
        </activity>
        <activity
            android:name=".bucket.BucketDetailActivity"
            android:label="@string/title_activity_bucket_detail" >
        </activity>
        <activity
            android:name=".pipeline.PipelineDetailActivity"
            android:label="@string/title_activity_pipeline_detail" >
        </activity>
        <activity
            android:name=".job.JobDetailActivity"
            android:label="@string/title_activity_job_detail" >
        </activity>
        <activity
            android:name=".BMCMainActivity"
            android:label="@string/title_activity_bmcmain" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".preset.PresetDetailActivity"
            android:label="@string/title_activity_preset_detail" ></activity>
        <activity
            android:name=".pipeline.PipelineCreateActivity"
            android:label="@string/title_activity_pipeline_create">
        </activity>
    </application>

</manifest>
